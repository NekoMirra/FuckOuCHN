# ========================================
# 使用说明：
# 1. 复制此文件并重命名为 .env
# 2. 填写你的账号和密码
# 3. 根据需要调整其他配置
# ========================================

########## 必填配置 ##########
_ACCOUNT=""          # 你的账号
_PASSWORD=""         # 你的密码

########## 可选配置 ##########
# _SLOW_MO_MIN=3000  # 最小执行间隔（毫秒）
# _SLOW_MO_MAX=9000  # 最大执行间隔（毫秒）
# _HEAD_LESS=1       # 是否启用无头模式，默认不启用，参数为空则显示浏览器窗口
_PLAY_RATE=8         # 视频播放倍速，默认倍速为 8
_TOTAL_POINTS=60     # 考试及格分数，AI 答题分数超过此值时会自动结束当前考试

########## 功能开关 ##########
# 视频刷课开关（1=开启，0=关闭），默认开启
_ENABLE_VIDEO=1
# 自动答题开关（1=开启，0=关闭），默认开启
_ENABLE_EXAM=1

########## AI 答题配置（可选） ##########
# 开启自动答题功能需要配置以下参数
_API=""              # AI 答题 API 接口，例如：https://api.openai.com/v1
_KEY=""              # API 密钥
_MODEL=""            # 模型名，例如：gpt-4o-mini
_Qps=1               # 每秒查询次数，默认为 1

########## 代理配置（可选，仅用于 AI 和 axios，浏览器不受影响） ##########
# _PROXY_HOST=127.0.0.1
# _PROXY_PORT=8080

########## 并发配置 ##########
# 并发刷课窗口数量：
# 0 = 自动（按课程数拆分，最多 6）；1..6 = 固定并发
_CONCURRENCY=1

########## 课程组选择（可选） ##########
# 解决启动时"要求输入序号但无法输入"的情况
# _GROUP_INDEX=0     # 0 表示全部课程组
# _GROUP_INDEX=9     # 表示选择列表中的第 9 个课程组（列表从 1 开始）
# _GROUP_TITLE=xxx   # 按标题模糊匹配（也支持正则，如 /英语|形势/）
# _NON_INTERACTIVE=1 # 强制非交互模式（不读取控制台输入，直接用默认策略）

########## 执行策略（可选） ##########
# 非交互/超时默认策略：只执行"进度最低的前 N 个课程组"
# _GROUP_LOWEST_N=3

# 默认执行策略：只执行"进度最低的前 N 个课程"（每个课程组内）
# - 不填时：开启视频刷课时默认处理全部未完成课程
# - 填 10：表示每个课程组只跑进度最低的 10 个
# - 填 0：表示处理全部
# _LOWEST_N=10

########## UI 配置 ##########
# 是否显示所有刷课窗口（0=只显示 W1，其余后台运行）
_SHOW_WORKERS=0

# 进度面板是否置顶（1=置顶，0=不置顶）
_UI_TOPMOST=1

# 调试 UI：开启后会打开进度窗口的 DevTools
# _DEBUG_UI=1
